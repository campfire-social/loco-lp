{% assign _url = nav_link %}
{% assign _text = nav_text %}
{% assign _target = nav_target | default: "_self" %}

<a class="nav-link inline-block hover:bg-black px-6 py-3 text-white uppercase text-xl md:text-lg font-bold tracking-wide cursor-pointer transition-colors" href="{{_url}}" target="{{_target}}">
  <span>{{_text}}</span> 
</a>